import {
	Box,
	Sticky
} from "@chakra-ui/react";

const Footer = props=>{
	return (
		<Box
			bottom="0"
			borderBottom="1px solid #ccc"
			padding="3px"
			backgroundColor="#ccc"
		>
			word homie this is the footer
		</Box>
	);
};

export default Footer;